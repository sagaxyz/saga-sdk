package types

const (
	// ModuleName defines the module name
	ModuleName = "transferrouter"

	// StoreKey defines the primary module store key
	StoreKey = ModuleName

	// RouterKey defines the message routing key for the module
	RouterKey = ModuleName
)
