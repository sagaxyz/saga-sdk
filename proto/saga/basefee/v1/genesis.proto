syntax = "proto3";
package saga.basefee.v1;

import "saga/basefee/v1/basefee.proto";
import "gogoproto/gogo.proto";

option go_package = "github.com/sagaxyz/saga-sdk/x/basefee/types";

// GenesisState defines the basefee module's genesis state.
message GenesisState {
  // params defines all the parameters of the basefee module.
  Params params = 1 [(gogoproto.nullable) = false];
}
